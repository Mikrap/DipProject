<Page x:Class="DiplomV3.Pages.CommandPages.EditRecordPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource PrimaryBackgroundColor}" Title="Редактировать запись">

    <Grid Margin="20">
        <Border Padding="20"
                CornerRadius="10"
                Background="{DynamicResource SecondaryBackgroundColor}"
                BorderBrush="{DynamicResource BorderColor}"
                BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Редактировать запись"
                           FontSize="18" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryForegroundColor}"
                           TextAlignment="Center" Margin="0,0,0,10"/>

                <StackPanel x:Name="DynamicFieldsPanel" Margin="0,10,0,0"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="✅ Сохранить" Click="BtnSave_Click" Width="110" Height="35"
                            Style="{StaticResource GitHubButtonStyle}"/>
                    <Button Content="❌ Отмена" Click="BtnCancel_Click" Width="95" Height="35" Margin="10,0,0,0"
                            Style="{StaticResource GitHubButtonStyle}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Page>